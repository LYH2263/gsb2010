# 库存筛选数据流与根因分析报告

## 一、完整数据流调用链

### 前端（React）→ 后端（Laravel）

#### 第 1 步：用户交互与状态更新
**文件**：[InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx)

| 操作 | 函数/状态 | 行号 | 说明 |
|------|-----------|------|------|
| 勾选「仅低库存」 | `setLowStockOnly(true)` | L127-L129 | checkbox 的 onChange 事件，更新 `lowStockOnly` 状态 |
| 点击「查询」按钮 | `handleSearch(e)` | L33-L39 | 表单提交，触发搜索 |

#### 第 2 步：应用筛选条件
**文件**：[InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx)

| 函数 | 行号 | 说明 |
|------|------|------|
| `handleSearch()` | L33-L39 | 将临时状态（`lowStockOnly`）同步到「已应用」状态（`appliedLowStockOnly`），同时重置页码为 1 |
| `useEffect` | L31 | 监听 `appliedLowStockOnly` 变化，触发 `load(page)` |

#### 第 3 步：构建请求参数并发送 API
**文件**：[InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx) + [api.js](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/api.js)

| 函数 | 行号 | 说明 |
|------|------|------|
| `load(p = page)` | L22-L28 | 构建 params：`{ per_page: 15, page: p, low_stock: '1' }`，调用 `getInventory(params)` |
| `getInventory(params)` | L229-L237 | 用 `URLSearchParams` 序列化参数，发送 `GET /api/inventory?low_stock=1&...` |

---

#### 第 4 步：后端路由匹配
**文件**：[api.php](file:///d:/Agsb/Agsb616/gsb2010/backend/routes/api.php) + [RouteServiceProvider.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Providers/RouteServiceProvider.php)

| 路由 | 行号 | 说明 |
|------|------|------|
| `GET /api/inventory` | api.php L40 | 匹配到 `InventoryController@index`，带 `auth:web` 中间件 |
| 路由前缀 | RouteServiceProvider L24-L26 | `api` 路由组统一加 `/api` 前缀，middleware 为 `['web', 'api']` |

#### 第 5 步：Controller 层参数解析
**文件**：[InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php)

| 代码段 | 行号 | 说明 |
|--------|------|------|
| 解析 `low_stock` | L29-L31 | 若 `$request->query('low_stock') === '1' \|\| === 'true'`，则 `$filters['low_stock'] = true` |
| 调用 list | L32 | `$this->inventoryService->list($perPage, ['filters' => $filters])` → **传入了筛选条件** |
| 调用 stats | L33 | `$this->inventoryService->stats()` → **未传入筛选条件** |

#### 第 6 步：Service 层查询构建
**文件**：[InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php)

| 方法 | 行号 | 说明 |
|------|------|------|
| `list()` | L13-L30 | 从 `$options['filters']` 取筛选条件，应用 `keyword`、`category_id`、`low_stock` 三个 where 条件，返回分页对象 |
| `stats()` | L45-L55 | **无任何筛选参数**，直接对全表 `products` 做 `sum`、聚合查询，返回全局统计 |

#### 第 7 步：返回响应与前端渲染
**文件**：[InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php) + [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx)

| 层 | 数据结构 | 说明 |
|----|----------|------|
| 后端返回 | `{ products: 分页对象, stats: 统计数组 }` | products 已筛选，stats 为全局 |
| 前端商品列表 | `data.products.data` | **筛选后的数据**，列表显示正确 |
| 前端统计卡片 | `data.stats` | **全局统计数据**，不随筛选变化 |
| 前端分页总数 | `data.products.total` | **筛选后的总数**，正确 |

---

## 二、根因分析

### 现象
勾选「仅低库存」并点击查询后：
- ✅ 商品列表正确筛选为仅低库存商品
- ✅ 分页总数正确反映筛选后的数量
- ❌ 顶部三张统计卡片（库存总量、库存总价值、低库存商品数）数值不变，始终显示全局统计

### 直接原因
[InventoryController.php 第 33 行](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L33-L33) 调用 `stats()` 时未传入 `$filters`。

### 根本原因
[InventoryService.php 第 45-55 行](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L45-L55) 的 `stats()` 方法本身设计上就**不接受任何筛选参数**，方法内部直接对全表 `products` 进行聚合查询，完全没有考虑筛选条件。

```php
// stats() 方法签名——没有 $filters 参数
public function stats(): array
{
    $totalStock = Product::sum('stock');           // 全表 sum
    $totalValue = DB::table('products')->...;      // 全表聚合
    $lowStockCount = Product::where('stock', '<=', 10)->count();  // 全表 count
    return [...];
}
```

相比之下，`list()` 方法正确接收并应用了筛选条件：
```php
public function list(int $perPage = 15, array $options = [])
{
    $filters = $options['filters'] ?? [];
    // ... 应用 keyword、category_id、low_stock 筛选
    if (!empty($filters['low_stock'])) {
        $q->where('stock', '<=', 10);
    }
}
```

### 涉及文件汇总

| 文件 | 问题位置 | 问题描述 |
|------|----------|----------|
| [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php) | L45-L55 `stats()` 方法 | 方法签名无筛选参数，内部查询未应用筛选条件 |
| [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php) | L33 | 调用 `stats()` 时未传递 `$filters` |

---

## 三、建议修复方案（最小改动）

### 方案概述
修改 2 个文件，约 15-20 行代码。

### 改动 1：`InventoryService::stats()` 增加筛选参数
**文件**：`backend/app/Services/InventoryService.php`

- 给 `stats()` 方法增加 `array $filters = []` 参数
- 复制 `list()` 方法中的筛选逻辑（keyword、category_id、low_stock），应用到 stats 的三个查询中
- 确保 `total_stock`、`total_value`、`low_stock_count` 都基于筛选后的数据集计算

### 改动 2：`InventoryController@index` 调用时传参
**文件**：`backend/app/Http/Controllers/InventoryController.php`

- 将第 33 行 `$stats = $this->inventoryService->stats();` 改为 `$stats = $this->inventoryService->stats($filters);`

### 可选优化（代码复用）
在 `InventoryService` 中抽取私有方法 `applyFilters($query, $filters)`，将 `list()` 和 `stats()` 中重复的筛选逻辑统一封装，避免两处维护、后续新增筛选条件时容易遗漏。

### 修复后预期效果
勾选「仅低库存」并查询后：
- 商品列表：仅显示库存 ≤ 10 的商品（已生效，无变化）
- 统计卡片-库存总量：仅统计筛选后商品的库存总和
- 统计卡片-库存总价值：仅统计筛选后商品的库存价值
- 统计卡片-低库存商品数：筛选后全部都是低库存，此数值应等于商品总数
- 分页总数：筛选后的商品数（已生效，无变化）
