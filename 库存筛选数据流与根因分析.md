# 库存筛选数据流与根因分析

## 一、调用链说明

### 前端 → 后端完整数据流

从用户勾选「仅低库存」并点击「查询」，到后端返回结果，完整数据流如下：

#### 1. 前端交互层

| 步骤 | 文件 | 函数/位置 | 说明 |
|------|------|-----------|------|
| 1 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L126-L133) | 复选框 `onChange` | 用户勾选「仅低库存」，更新 `lowStockOnly` state |
| 2 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L33-L39) | `handleSearch()` | 点击「查询」，设置 `appliedLowStockOnly` 并重置页码 |
| 3 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L31) | `useEffect` | 监听 `appliedLowStockOnly` 变化，触发 `load()` |
| 4 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L22-L28) | `load()` | 构建请求参数，`low_stock = '1'`，调用 `getInventory()` |
| 5 | [api.js](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/api.js#L229-L237) | `getInventory()` | 发送 `GET /api/inventory?low_stock=1&per_page=15&page=1` |

#### 2. 后端处理层

| 步骤 | 文件 | 函数/位置 | 说明 |
|------|------|-----------|------|
| 6 | [api.php](file:///d:/Agsb/Agsb616/gsb2010/backend/routes/api.php#L40) | 路由定义 | `GET /api/inventory` → `InventoryController@index` |
| 7 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L17-L41) | `index()` | 解析查询参数，构建 `$filters` 数组 |
| 8 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L32) | `index()` | 调用 `inventoryService->list($perPage, ['filters' => $filters])` |
| 9 | [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L13-L30) | `list()` | 应用 `low_stock` 筛选条件，返回分页结果 ✅ **筛选生效** |
| 10 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L33) | `index()` | 调用 `inventoryService->stats()` ❌ **未传筛选参数** |
| 11 | [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L45-L55) | `stats()` | 无筛选参数，统计全表数据 ❌ **筛选不生效** |
| 12 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L34-L38) | `index()` | 返回 JSON：`{ products, stats }` |

#### 3. 前端渲染层

| 步骤 | 文件 | 位置 | 说明 |
|------|------|------|------|
| 13 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L54-L60) | 数据解构 | `products` 来自筛选后的列表，`stats` 来自全局统计 |
| 14 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L69-L85) | 统计卡片 | 展示 `stats.total_stock`、`stats.total_value`、`stats.low_stock_count`（全局数据，与筛选不一致） |
| 15 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L149-L176) | 商品列表 | 展示筛选后的 `products`（正确） |

---

## 二、根因分析

### 核心问题：`stats()` 方法未应用筛选条件

筛选不生效的根因在于 **统计数据（stats）与列表数据（products）使用了不一致的筛选逻辑**：

#### 具体表现

- ✅ **商品列表筛选正常**：`InventoryService::list()` 方法正确接收并应用了 `$filters` 参数，包括 `keyword`、`category_id`、`low_stock` 三个筛选条件。
- ❌ **统计数据筛选失效**：`InventoryService::stats()` 方法**不接收任何筛选参数**，总是对全表 `products` 进行统计。

#### 关键代码位置

**1. Controller 层遗漏传参**

[InventoryController.php 第 33 行](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L33)：

```php
$stats = $this->inventoryService->stats();  // ❌ 未传入 $filters
```

对比第 32 行的 `list()` 调用：

```php
$products = $this->inventoryService->list($perPage, ['filters' => $filters]);  // ✅ 传入了 filters
```

**2. Service 层无筛选逻辑**

[InventoryService.php 第 45-55 行](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L45-L55)：

```php
public function stats(): array
{
    $totalStock = Product::sum('stock');  // ❌ 全表求和，无筛选
    $totalValue = DB::table('products')->selectRaw('SUM(price * stock) as v')->value('v') ?? 0;  // ❌ 全表计算
    $lowStockCount = Product::where('stock', '<=', 10)->count();  // ❌ 全表统计低库存
    return [
        'total_stock' => (int) $totalStock,
        'total_value' => round((float) $totalValue, 2),
        'low_stock_count' => $lowStockCount,
    ];
}
```

#### 导致的现象

当用户勾选「仅低库存」并查询时：
- 商品列表正确显示库存 ≤ 10 的商品（筛选生效）
- 但顶部三个统计卡片（库存总量、库存总价值、低库存商品数）显示的仍然是**全部商品**的统计数据
- 统计数据与列表数据不一致，用户误以为筛选未生效

---

## 三、建议修复方案

### 最小改动方案

改动涉及 **2 个文件**，约 **20 行代码**：

#### 改动 1：`InventoryService::stats()` 添加筛选支持

**文件**：[InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php)

**改动内容**：
- 给 `stats()` 方法添加 `$filters` 参数（可选，默认空数组）
- 在三个统计查询中应用与 `list()` 相同的筛选条件（keyword、category_id、low_stock）
- 建议抽取一个私有方法 `applyFilters($query, $filters)` 来复用筛选逻辑，避免重复代码

#### 改动 2：`InventoryController@index` 传入筛选参数

**文件**：[InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php)

**改动内容**：
- 将第 33 行的 `$stats = $this->inventoryService->stats();` 改为传入 `$filters`

### 涉及文件汇总

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php) | 修改 `stats()` 方法签名和实现 | 添加 `$filters` 参数，应用筛选逻辑 |
| [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php) | 修改 `index()` 方法调用 | 调用 `stats()` 时传入 `$filters` |

### 可选优化：抽取公共筛选逻辑

为避免 `list()` 和 `stats()` 中重复的筛选代码，可在 `InventoryService` 中抽取一个私有辅助方法：

```php
private function applyFilters($query, array $filters)
{
    if (!empty($filters['keyword'])) {
        $kw = trim($filters['keyword']);
        $query->where(function ($q) use ($kw) {
            $q->where('name', 'like', '%' . $kw . '%')->orWhere('sku', 'like', '%' . $kw . '%');
        });
    }
    if (isset($filters['category_id']) && $filters['category_id'] !== '' && $filters['category_id'] !== null) {
        $query->where('category_id', (int) $filters['category_id']);
    }
    if (!empty($filters['low_stock'])) {
        $query->where('stock', '<=', 10);
    }
}
```

然后 `list()` 和 `stats()` 都调用此方法，确保筛选逻辑一致性。
