# 库存「仅低库存」筛选失效问题分析报告

> 分析方式：仅阅读源码，未运行任何服务、未修改任何源代码文件。
> 涉及仓库：Laravel（backend）+ React/Vite（frontend）

---

## 一、调用链说明（前端勾选「仅低库存」→ 点击「查询」→ 后端返回）

### 1. 前端：用户交互与状态更新

**文件：`frontend/src/pages/InventoryList.jsx`**

- 复选框状态：`lowStockOnly`（[InventoryList.jsx:17](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L17)）
  - 勾选时通过 `onChange={(e) => setLowStockOnly(e.target.checked)}` 更新（[InventoryList.jsx:129](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L129)）。
- 点击「查询」触发 `handleSearch`（[InventoryList.jsx:33-39](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L33-L39)）：
  - 调用 `setAppliedLowStockOnly(lowStockOnly)`，把“待应用”状态写入 `appliedLowStockOnly`；
  - 同时 `setPage(1)` 重置到第一页。
- `useEffect` 监听 `appliedLowStockOnly`（[InventoryList.jsx:31](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L31)），变化后调用 `load(page)`。
- `load` 构造请求参数（[InventoryList.jsx:22-28](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L22-L28)），关键行：
  ```js
  if (appliedLowStockOnly) params.lowStock = '1'   // ← 参数名为 camelCase：lowStock
  ```
  随后调用 `getInventory(params)`。

### 2. 前端：API 封装层

**文件：`frontend/src/api.js`**

- `getInventory`（[api.js:229-237](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/api.js#L229-L237)）：
  ```js
  const q = new URLSearchParams(params).toString();
  const r = await fetch(BASE + '/inventory' + (q ? '?' + q : ''), { ... });
  ```
  - `URLSearchParams` 会原样保留键名，最终发出：
    `GET /api/inventory?per_page=15&page=1&lowStock=1`
  - 注意：实际请求里携带的是 **`lowStock=1`**（camelCase）。

### 3. 后端：路由 → 控制器

**文件：`backend/routes/api.php`**

- `Route::get('inventory', [InventoryController::class, 'index']);`（[api.php:40](file:///d:/Agsb/Agsb616/gsb2010/backend/routes/api.php#L40)）。

**文件：`backend/app/Http/Controllers/InventoryController.php`**

- `index`（[InventoryController.php:17-41](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L17-L41)）解析筛选条件：
  ```php
  if ($request->query('low_stock') === '1' || $request->query('low_stock') === 'true') {
      $filters['low_stock'] = true;   // ← 读取的是 snake_case：low_stock
  }
  ```
  - 由于请求中只有 `lowStock`，没有 `low_stock`，`$request->query('low_stock')` 返回 `null`，条件不成立 → **`$filters['low_stock']` 从未被设置**。
- 随后调用 `$this->inventoryService->list($perPage, ['filters' => $filters])`（[InventoryController.php:32](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L32)）。

### 4. 后端：Service 层查询

**文件：`backend/app/Services/InventoryService.php`**

- `list`（[InventoryService.php:13-30](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L13-L30)）：
  ```php
  if (!empty($filters['low_stock'])) {
      $q->where('stock', '<=', 10);   // ← 真正执行低库存过滤的地方
  }
  ```
  - 因 `$filters['low_stock']` 未设置，`!empty(...)` 为 `false`，该 `where` 子句被跳过。
- 最终 `$q->paginate($perPage)` 返回**全量分页数据**（未按库存过滤）。

### 5. 返回链路

- Controller 把 `products`（分页器）与 `stats`（统计）以 JSON 返回（[InventoryController.php:34-39](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L34-L39)）。
- 前端 `getInventory` 解析 JSON → `setData`（[InventoryList.jsx:27](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L27)）→ 表格渲染全量商品，表现为「仅低库存」筛选无效。

### 调用链一览

```
[InventoryList.jsx] 复选框 lowStockOnly
        │ handleSearch() → setAppliedLowStockOnly(true)
        ▼
[InventoryList.jsx] load() → params.lowStock = '1'   ← camelCase
        ▼
[api.js] getInventory() → GET /api/inventory?...&lowStock=1
        ▼
[api.php] Route::get('inventory') → InventoryController@index
        ▼
[InventoryController.php] $request->query('low_stock')  ← snake_case，取不到值
        │ $filters['low_stock'] 未设置
        ▼
[InventoryService.php] !empty($filters['low_stock']) 为 false
        │ 跳过 where('stock','<=',10)
        ▼
返回全量分页数据 → 前端显示未筛选结果
```

---

## 二、根因分析

**根因：前后端查询参数命名不一致（camelCase vs snake_case），导致后端永远读不到该筛选标志。**

- 前端发出的参数键为 `lowStock`（camelCase），见 [InventoryList.jsx:26](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L26)。
- 后端读取的参数键为 `low_stock`（snake_case），见 [InventoryController.php:29](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L29)。
- Laravel 的 `$request->query('low_stock')` 不会自动把 `lowStock` 转换为 `low_stock`，因此始终返回 `null`，`$filters['low_stock']` 永远缺失，`InventoryService::list` 中的 `where('stock', '<=', 10)` 永远不执行。

**佐证（同模块其它参数均一致使用 snake_case，唯独此项不一致）：**

| 参数 | 前端键名 | 后端读取键名 | 是否一致 | 结果 |
|------|----------|--------------|----------|------|
| 每页数 | `per_page` | `per_page` | ✅ | 正常 |
| 页码 | `page` | `page` | ✅ | 正常 |
| 关键词 | `keyword` | `keyword` | ✅ | 正常 |
| 分类 | `category_id` | `category_id` | ✅ | 正常 |
| **仅低库存** | **`lowStock`** | **`low_stock`** | ❌ | **失效** |

可对照 `ProductController::index`（[ProductController.php:19-25](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/ProductController.php#L19-L25)），后端整体遵循 snake_case 约定；前端 `InventoryList.jsx` 对 `category_id`、`per_page` 也用 snake_case，唯独 `lowStock` 用了 camelCase，属局部笔误。

### 涉及文件与原因表

| 文件 | 关键位置 | 问题/原因 |
|------|----------|-----------|
| `frontend/src/pages/InventoryList.jsx` | 第 26 行 `params.lowStock = '1'` | 参数键写成 camelCase `lowStock`，与后端不一致 |
| `backend/app/Http/Controllers/InventoryController.php` | 第 29 行 `$request->query('low_stock')` | 读取 snake_case `low_stock`，匹配不到前端传入的 `lowStock` |
| `backend/app/Services/InventoryService.php` | 第 26-28 行 `if (!empty($filters['low_stock']))` | 因上游未传入 `low_stock`，过滤条件被跳过（非本文件 bug，是受害者） |

---

## 三、建议修复方案（最小改动）

**方案：统一为 snake_case，仅改前端一行。**

在 `frontend/src/pages/InventoryList.jsx` 第 26 行，将

```js
if (appliedLowStockOnly) params.lowStock = '1'
```

改为

```js
if (appliedLowStockOnly) params.low_stock = '1'
```

- 改动范围：1 个文件、1 行、1 个标识符。
- 选择改前端的理由：
  1. 后端 `InventoryController` / `InventoryService` / `ProductController` 全链路均使用 snake_case，符合 Laravel 生态约定，不应为单个参数破例；
  2. 前端同文件内 `category_id`、`per_page` 已是 snake_case，改 `low_stock` 后风格统一；
  3. 改动最小、风险最低，无需触碰后端逻辑与数据库。

**备选方案（不推荐）**：改后端 `InventoryController.php:29` 为 `$request->query('lowStock')`。能修复但破坏后端 snake_case 一致性，且需同步检查 `InventoryService` 的 `$filters['low_stock']` 键名，改动点更多、易遗漏。

**验证要点（修复后）**：勾选「仅低库存」→ 查询，请求应出现 `low_stock=1`，返回列表仅含 `stock <= 10` 的商品，分页 `total` 与 `stats.low_stock_count` 口径一致（注意 `stats` 始终是全局统计，不受筛选影响，属预期行为）。
