# 库存筛选问题分析报告

> 分析方式：仅通过阅读源码进行静态分析
> 分析范围：`frontend/src/pages/InventoryList.jsx`、`frontend/src/api.js`、后端库存相关 Controller / Service

---

## 一、调用链说明

从前端勾选「仅低库存」并点击「查询」，到后端返回筛选结果，完整数据流经过以下文件与函数：

### 前端调用链

| 序号 | 文件 | 函数/方法 | 行号 | 说明 |
|------|------|-----------|------|------|
| 1 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L125-L133) | 复选框 onChange | L126-L131 | 用户勾选「仅低库存」，触发 `setLowStockOnly(e.target.checked)` |
| 2 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L33-L39) | `handleSearch()` | L33-L39 | 点击「查询」按钮，设置 `appliedLowStockOnly = lowStockOnly` 并重置页码为 1 |
| 3 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L31) | `useEffect` | L31 | 监听 `appliedLowStockOnly` 等依赖变化，触发 `load(page)` |
| 4 | [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L22-L28) | `load()` | L22-L28 | 构建请求参数，`appliedLowStockOnly` 为 true 时设置 `params.low_stock = '1'` |
| 5 | [api.js](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/api.js#L229-L237) | `getInventory()` | L229-L237 | 用 `URLSearchParams` 序列化参数，发送 GET 请求到 `/api/inventory` |

### 后端调用链

| 序号 | 文件 | 函数/方法 | 行号 | 说明 |
|------|------|-----------|------|------|
| 1 | [api.php](file:///d:/Agsb/Agsb616/gsb2010/backend/routes/api.php#L40) | 路由匹配 | L40 | `GET /inventory` 路由到 `InventoryController@index`（在 `auth:web` 中间件组内） |
| 2 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L17-L41) | `index()` | L17-L41 | 解析查询参数，构建 `$filters` 数组 |
| 3 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L29-L31) | low_stock 参数解析 | L29-L31 | 判断 `low_stock` 是否为 `'1'` 或 `'true'`，若是则设置 `$filters['low_stock'] = true` |
| 4 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L32) | 调用 Service 列表 | L32 | `$this->inventoryService->list($perPage, ['filters' => $filters])` |
| 5 | [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L13-L30) | `list()` | L13-L30 | 根据 `$filters` 构建查询，low_stock 为真时添加 `where('stock', '<=', 10)` 条件 |
| 6 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L33) | 调用 Service 统计 | L33 | `$this->inventoryService->stats()` —— **未传递任何筛选参数** |
| 7 | [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L45-L55) | `stats()` | L45-L55 | 计算全局库存统计（无筛选条件） |
| 8 | [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L34-L39) | 返回 JSON | L34-L39 | 返回 `{ products: 分页器, stats: 统计数据 }` |

---

## 二、根因分析

### 🔴 根因一（核心）：`stats()` 方法未应用筛选条件，统计数据与列表脱节

**涉及文件**：
- [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php#L45-L55) — `stats()` 方法
- [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php#L33) — 调用处

**关键代码**：

```php
// InventoryController@index
$products = $this->inventoryService->list($perPage, ['filters' => $filters]);
$stats = $this->inventoryService->stats();  // ⚠️ 没有传递筛选参数
```

`stats()` 方法的实现：

```php
// InventoryService@stats
public function stats(): array
{
    $totalStock = Product::sum('stock');                  // 全局总和
    $totalValue = DB::table('products')
        ->selectRaw('SUM(price * stock) as v')->value('v') ?? 0;  // 全局总价值
    $lowStockCount = Product::where('stock', '<=', 10)->count();  // 全局低库存数
    return [
        'total_stock' => (int) $totalStock,
        'total_value' => round((float) $totalValue, 2),
        'low_stock_count' => $lowStockCount,
    ];
}
```

**问题说明**：

当用户勾选「仅低库存」并点击查询后：

| 数据项 | 是否随筛选变化 | 说明 |
|--------|---------------|------|
| 商品列表（products.data） | ✅ 变化 | 正确应用了 `where stock <= 10` 条件 |
| 列表分页总数（products.total） | ✅ 变化 | `paginate()` 自动按筛选条件计算总数 |
| 顶部统计-库存总量（stats.total_stock） | ❌ 不变 | 始终是全部商品的库存总和 |
| 顶部统计-库存总价值（stats.total_value） | ❌ 不变 | 始终是全部商品的库存总价值 |
| 顶部统计-低库存商品数（stats.low_stock_count） | ❌ 不变 | 始终是全部商品中的低库存数量 |

用户看到顶部三个统计卡片的数字在筛选前后完全一致，很容易直观地认为"筛选没生效"。这是最可能导致用户反馈"筛选不生效"的根因。

---

### 🟡 问题二（代码冗余）：前端 `lowStockList` 过滤逻辑重复

**涉及文件**：
- [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx#L57)

```js
const lowStockList = products.filter((p) => p.stock <= 10)
```

如果后端筛选正确生效，`products` 中的所有商品都是低库存的，这个前端过滤就是冗余的。反之，如果要依靠前端过滤来实现"低库存提醒"，则说明后端筛选可能没生效。

---

### 🔵 补充说明：商品列表筛选逻辑本身是正确的

从代码静态分析来看，**商品列表的低库存筛选逻辑链条是完整且正确的**：

1. 前端：`appliedLowStockOnly` → `params.low_stock = '1'`
2. 后端 Controller：`$request->query('low_stock') === '1'` → `$filters['low_stock'] = true`
3. 后端 Service：`!empty($filters['low_stock'])` → `$q->where('stock', '<=', 10)`

每一步的条件判断和参数传递都是通顺的。如果商品列表确实完全没有被筛选，需要进一步排查运行时问题（如参数类型、中间件影响等），但仅从源码静态分析无法确认。

---

## 三、建议修复方案

### 方案一（最小改动）：让 `stats()` 接受筛选参数

**改动思路**：为 `stats()` 方法添加筛选参数支持，使其与 `list()` 方法使用相同的筛选条件，保证统计数据与列表数据一致。

**涉及文件**：
1. `backend/app/Services/InventoryService.php` — 修改 `stats()` 方法
2. `backend/app/Http/Controllers/InventoryController.php` — 调用时传递 `$filters`

**具体修改**：

**1. InventoryService.php** — 改造 `stats()` 方法：

```php
/**
 * @param array{keyword?: string, category_id?: int, low_stock?: bool} $filters
 */
public function stats(array $filters = []): array
{
    $q = Product::query();

    // 应用关键字筛选
    if (!empty($filters['keyword'])) {
        $kw = trim($filters['keyword']);
        $q->where(function ($q) use ($kw) {
            $q->where('name', 'like', '%' . $kw . '%')
              ->orWhere('sku', 'like', '%' . $kw . '%');
        });
    }

    // 应用分类筛选
    if (isset($filters['category_id']) 
        && $filters['category_id'] !== '' 
        && $filters['category_id'] !== null) {
        $q->where('category_id', (int) $filters['category_id']);
    }

    // 应用低库存筛选
    if (!empty($filters['low_stock'])) {
        $q->where('stock', '<=', 10);
    }

    $totalStock = (clone $q)->sum('stock');
    $totalValue = (clone $q)->selectRaw('SUM(price * stock) as v')->value('v') ?? 0;
    $lowStockCount = (clone $q)->where('stock', '<=', 10)->count();

    return [
        'total_stock' => (int) $totalStock,
        'total_value' => round((float) $totalValue, 2),
        'low_stock_count' => $lowStockCount,
    ];
}
```

**2. InventoryController.php** — 传递筛选参数：

将第 33 行：
```php
$stats = $this->inventoryService->stats();
```
改为：
```php
$stats = $this->inventoryService->stats($filters);
```

---

### 方案二（推荐优化）：抽取筛选逻辑，代码复用

将筛选条件的构建逻辑抽取为一个私有方法（如 `applyFilters($query, $filters)`），在 `list()` 和 `stats()` 中复用，避免重复代码，降低维护成本。

```php
private function applyFilters($query, array $filters)
{
    if (!empty($filters['keyword'])) {
        $kw = trim($filters['keyword']);
        $query->where(function ($q) use ($kw) {
            $q->where('name', 'like', '%' . $kw . '%')
              ->orWhere('sku', 'like', '%' . $kw . '%');
        });
    }
    if (isset($filters['category_id']) 
        && $filters['category_id'] !== '' 
        && $filters['category_id'] !== null) {
        $query->where('category_id', (int) $filters['category_id']);
    }
    if (!empty($filters['low_stock'])) {
        $query->where('stock', '<=', 10);
    }
    return $query;
}
```

然后 `list()` 和 `stats()` 都调用这个方法来应用筛选。

---

## 四、涉及文件汇总

| 文件 | 问题/角色 | 严重程度 |
|------|-----------|----------|
| [InventoryService.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Services/InventoryService.php) | `stats()` 方法未接受筛选参数，统计数据全局化 | 🔴 高 |
| [InventoryController.php](file:///d:/Agsb/Agsb616/gsb2010/backend/app/Http/Controllers/InventoryController.php) | 调用 `stats()` 时未传递筛选条件 | 🔴 高 |
| [InventoryList.jsx](file:///d:/Agsb/Agsb616/gsb2010/frontend/src/pages/InventoryList.jsx) | 前端 `lowStockList` 过滤逻辑冗余 | 🟡 低 |

---

*报告生成时间：基于源码静态分析，未运行项目*
